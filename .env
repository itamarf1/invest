# Google OAuth Configuration
# To get this: Go to console.cloud.google.com > APIs & Services > Credentials
# Create OAuth client ID > Web application > Add http://localhost:8000 as authorized origin
# Copy the Client ID (ends with .apps.googleusercontent.com)
GOOGLE_CLIENT_ID=

# Database Configuration
# If using SQLite (default), leave empty or set path
# Example: DATABASE_URL=sqlite:///./investment.db
DATABASE_URL=

# API Keys for Data Sources
# Alpha Vantage: Get free key at https://www.alphavantage.co/support/#api-key
# Used for real-time stock data
ALPHA_VANTAGE_API_KEY=

# Yahoo Finance (usually no key needed, but some services require it)
# Most Yahoo Finance APIs are free without key
YAHOO_FINANCE_API_KEY=

# Security Configuration  
# Generate random 32+ character string for JWT tokens
# Use: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Session timeout in hours (default: 24)
SESSION_TIMEOUT_HOURS=24

# Rate limiting
# Max requests per minute per IP (default: 60)
RATE_LIMIT_PER_MINUTE=60

# Server Configuration
# Port to run the server (default: 8000)
PORT=8000

# Environment (development, staging, production)
ENVIRONMENT=development

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS settings - allowed origins (comma separated)
# For development: http://localhost:3000,http://localhost:8080
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8000